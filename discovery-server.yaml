server:
  port: 8761

eureka:
  client:
    register-with-eureka: false   # Le serveur ne s’enregistre pas lui-même
    fetch-registry: false          # Il ne récupère pas de registre
  server:
    enable-self-preservation: false
